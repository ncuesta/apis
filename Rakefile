require 'bundler/setup'

namespace :api do
  desc 'Inicia la API de Alumnos'
  task :alumnos do
    %x{ rackup -p 3000 alumnos.rb }
  end

  desc 'Inicia la API de Becas'
  task :becas do
  end

  desc 'Inicia la API del ANSES'
  task :anses do
  end
end
